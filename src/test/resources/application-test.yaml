spring:
    datasource:
        url: jdbc:sqlite:./build/test-wheresmymoney.db
        driver-class-name: org.sqlite.JDBC
    jpa:
        hibernate:
            ddl-auto: create-drop
        properties:
            hibernate.dialect: org.hibernate.community.dialect.SQLiteDialect
        open-in-view: false

# Sem rede nos testes: busca usa só fallback textual.
ollama:
    enabled: false
